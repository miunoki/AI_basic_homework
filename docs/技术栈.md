# 技术栈

本文档记录项目当前使用的主要技术、关键实现方案和技术变更记录。后续凡是涉及检索、生成、认证、界面、部署、数据处理等技术方案的修改，都需要同步更新本文档。

## 维护规则

- 新增技术能力时，在“当前技术栈”中补充对应条目。
- 修改已有技术方案时，更新对应说明，并在“技术变更记录”中追加一条记录。
- 废弃或替换技术时，说明替换原因和新方案。
- 只记录技术层面的变化；普通文案、截图、报告排版等非技术修改不需要记录。

## 当前技术栈

| 模块 | 技术/方案 | 说明 |
|------|-----------|------|
| 编程语言 | Python | 项目主体逻辑、命令行入口、网页入口、检索与实验脚本均使用 Python 实现。 |
| 大模型调用 | DeepSeek API + OpenAI Python SDK | `llm.py` 使用 OpenAI SDK 兼容接口调用 `deepseek-chat`，支持从 `config.py` 或环境变量读取 API Key。 |
| RAG 架构 | 检索增强生成 | 用户提问后先从本地 CC98 知识库检索相关帖子，再将参考内容拼接进 Prompt，由大模型生成回答。 |
| 知识库 | 本地文本文件 | `knowledge/` 下每个 `.txt` 文件作为一个知识条目，内容来自 CC98 论坛帖子。 |
| 关键词检索 | 校园同义词扩展 + 中文 2-gram + Jaccard + 标题加权 + 低相关阈值 | 默认检索模式，无需额外模型；同义词表分为地点扩展和主题扩展，覆盖报到、宿舍、饮食、医疗、选课、网络、交通、体育、租房等新生高频场景；检索时过滤“怎么/什么/请问”等泛问题词，并对标题命中具体主题词的帖子额外加权；关键词分数低于 50 的结果会被过滤。 |
| 语义检索 | sentence-transformers | 可选进阶模式，使用多语言向量模型做语义相似度检索，并支持 embedding 缓存和失败回退。 |
| 上下文增强检索 | 追问识别 + 最近话题状态 | CLI 和网页端都会为短追问或包含“这个/那个/还有/几个/怎么去”等指代词的问题增强检索 query；增强内容包括上一轮用户问题、上一轮命中的来源标题和回答摘要，不增加额外模型调用。 |
| Prompt 构造 | top-8 召回 + top-4 引用 + 程序化来源追加 | 检索阶段召回 8 条候选帖子，Prompt 中引用前 4 条；每条资料最多保留 900 字，兼顾信息覆盖和上下文长度；回答末尾的参考来源由程序根据实际进入 Prompt 的检索结果统一追加，不依赖模型生成。 |
| 命令行界面 | 交互式 CLI | `main.py` 提供终端问答入口，支持多轮历史上下文。 |
| 网页界面 | Gradio Blocks | `app.py` 提供浏览器聊天界面、示例问题、访问控制和自动打开浏览器。 |
| 检索调试面板 | Gradio Accordion + Markdown | 网页端提供默认折叠的“检索过程 / 来源调试”面板，展示本轮原始问题、实际检索 query、检索模式、阈值、召回标题、相关度分数和是否进入 Prompt，便于定位 RAG 跑偏原因。 |
| 网页访问控制 | 访问码 / 个人 API Key 双通道 | 新生可输入统一访问码使用服务端 API；已有个人 DeepSeek API Key 的用户可直接输入 Key，无需访问码。 |
| 配置管理 | `config.py` + 环境变量 | 服务端 API Key 和访问码可通过 `config.py` 或 `DEEPSEEK_API_KEY`、`ACCESS_CODE` 环境变量配置。 |
| 启动方式 | `start_web.bat` | Windows 下可双击启动网页服务，降低非开发者使用门槛。 |
| 对比实验 | RAG vs 无知识库 | `compare.py` 使用固定测试问题对比带知识库和不带知识库的回答效果。 |
| 依赖管理 | `requirements.txt` | 记录 OpenAI SDK、sentence-transformers、Gradio、requests 等依赖。 |

## 技术变更记录

| 日期 | 变更 | 说明 |
|------|------|------|
| 2026-06-09 | 新增检索调试折叠面板 | 网页端新增默认折叠的检索调试区域；关键词检索模式下复用正式检索排序结果展示分数、阈值和进入 Prompt 状态，帮助区分检索跑偏与模型回答问题。 |
| 2026-06-06 | 增强多轮上下文检索 | 新增 `context_utils.py`，统一 CLI 和网页端的追问识别、最近来源标题保存和检索 query 增强逻辑；网页版新增隐藏会话状态保存上一轮检索上下文，改善第二问、短追问和指代性问题的命中率。 |
| 2026-06-06 | 新增程序化来源展示 | 回答生成后由程序基于实际进入 Prompt 的检索结果追加参考来源，避免模型漏写、改写或编造来源；CLI、网页和对比实验统一使用该逻辑。 |
| 2026-06-04 | 新增技术栈文档 | 建立 `docs/技术栈.md`，集中记录项目技术方案和后续技术变更。 |
| 2026-06-04 | 修复 Gradio 多轮历史格式兼容 | 网页端在进入模型前将 Gradio 返回的结构化历史消息统一转成纯文本；`llm.py` 同步增加非字符串内容清洗兜底，避免第二轮对话触发 `ord()` 类型错误。 |
| 2026-06-04 | 新增低相关阈值 | 关键词检索增加最低分数阈值，低于 50 分的候选帖不进入 RAG；无候选结果时直接返回“资料库中暂未找到相关信息”，避免模型基于弱相关帖子硬答。 |
| 2026-06-04 | 扩展校园同义词表 | 将同义词表扩展为地点组和主题组，新增报到、医保、心理、空调热水、外卖交通、教务学在、体育场馆、租房、证件等新生高频问法；同时加入问题停用词过滤和具体标题词加权，降低“怎么用”等泛词带来的跑偏。 |
| 2026-06-04 | 新增校园同义词扩展表 | 在 `retriever.py` 中增加同义词组，检索前自动扩展 query，并加入主题词标题加权，改善“牙科/口腔科”“宿舍/寝室”“ZJG/紫金港”等不同说法的命中率。 |
| 2026-06-04 | 扩大 RAG 检索召回范围 | 将 CLI、网页和对比实验的检索召回从默认 top-3 / Prompt top-2 调整为召回 top-8、Prompt 引用 top-4，并缩短单条资料截断长度。 |
| 2026-06-04 | 新增访问码 / 个人 API Key 双通道 | 网页端支持新生输入统一访问码使用服务端 API，也保留个人 DeepSeek API Key 直接使用方式。 |
| 2026-06-04 | 新增双击启动脚本 | 增加 `start_web.bat`，并让 Gradio 启动后自动打开浏览器，降低启动门槛。 |
| 2026-06-03 | 新增上下文增强检索 | CLI 多轮对话中，短追问会拼接上一轮问题进行检索，提升连续追问的相关性。 |
